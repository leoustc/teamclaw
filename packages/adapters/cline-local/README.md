# cline-local

Tracked home for the local wrapper used by the `cline_local` adapter flow.

The tracked wrapper script is `bin/cline-local`. TeamClaw bootstrap can expose it as the runtime command expected by the adapter flow for local agent execution inside the shared ticketing system.

The wrapper delegates to `cline`.
